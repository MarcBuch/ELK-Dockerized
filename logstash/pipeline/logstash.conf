## Input
input {
    tcp {
        port => 5000
    }
}

## Filters and Plugins configuration go here


## Output
output {
    elasticsearch {
        hosts => "http://elasticsearch:9200"
        user => "elastic"
        password => "changeme"
    }
}
